{% extends 'base.html.twig' %}

{% block title %}Beruf{% endblock %}

{% block body %}
    <h1>Beruf</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ beruf.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('beruf_index') }}">back to list</a>

    <a href="{{ path('beruf_edit', {'id': beruf.id}) }}">edit</a>

    {{ include('beruf/_delete_form.html.twig') }}
{% endblock %}
