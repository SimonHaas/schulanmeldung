{{ form_start(form) }}
{{ form_widget(form) }}
<input type="submit" />
{{ form_end(form) }}
{# TODO wird das irgendwo verwendet? #}
{# TODO jetzt können wir auch mal die unnötigen Actions (Methoden in Controllern) und Templates aufräumen #}