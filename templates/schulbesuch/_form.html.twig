{{ form_start(form) }}

<div class="row">
    <div class="col-lg-12">
        <label class="labelFont">Schule</label>
        {{  form_widget(form.schule, {'attr':{'class':'form-control'}}) }}
    </div>
</div>

<a class="btn controlButton btn-sm smallButtonDistance blankLinkButton" href="{{ path('schule_new') }}">Neue Schule anlegen</a>



<div class="row">
    <div class="col">
        <label class="labelFont">Eintritt</label>
        {{  form_widget(form.eintritt, {'attr':{'class':'form-control'}}) }}
    </div>
    <div class="col">
        <label class="labelFont">Austritt</label>
        {{  form_widget(form.austritt, {'attr':{'class':'form-control'}}) }}
    </div>
</div>

<br>
<br>

    <button class="btn controlButton buttonFont btn-block blankLinkButton">{{ button_label|default('Speichern') }}</button>

{{ form_end(form) }}
