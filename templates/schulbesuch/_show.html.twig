<h1>Schulbesuch</h1>

<table class="table">
    <tbody>
    <tr>
        <th>Eintritt</th>
        <td>{{ schulbesuch.eintritt ? schulbesuch.eintritt|date('Y-m-d') : '' }}</td>
    </tr>
    <tr>
        <th>Austritt</th>
        <td>{{ schulbesuch.austritt ? schulbesuch.austritt|date('Y-m-d') : '' }}</td>
    </tr>
    {# TODO an finale Datenstruktur anpassen #}
    </tbody>
</table>