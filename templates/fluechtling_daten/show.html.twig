{% extends 'base.html.twig' %}

{% block title %}FluechtlingDaten{% endblock %}

{% block body %}
    <h1>FluechtlingDaten</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fluechtling_daten.id }}</td>
            </tr>
            <tr>
                <th>DeutschKenntnis</th>
                <td>{{ fluechtling_daten.deutschKenntnis ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>AnmeldeStelle</th>
                <td>{{ fluechtling_daten.anmeldeStelle }}</td>
            </tr>
            <tr>
                <th>AnsprechPartner</th>
                <td>{{ fluechtling_daten.ansprechPartner }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ fluechtling_daten.tel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fluechtling_daten_index') }}">back to list</a>

    <a href="{{ path('fluechtling_daten_edit', {'id': fluechtling_daten.id}) }}">edit</a>

    {{ include('fluechtling_daten/_delete_form.html.twig') }}
{% endblock %}
