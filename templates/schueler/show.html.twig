{% extends 'base.html.twig' %}

{% block title %}Schueler{% endblock %}

{% block body %}
    {{ include('schueler/_show.html.twig') }}

    <a href="{{ path('schueler_index') }}">back to list</a>

    <a href="{{ path('schueler_edit', {'id': schueler.id}) }}">edit</a>

    {{ include('schueler/_delete_form.html.twig') }}
{% endblock %}
