{% extends 'base-admin.html.twig' %}

{% block title %}Verwaltung der Anmeldungen{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="conatiner example-wrapper">
        <div class="row">
            <div class="col-12">
                <a class="btn btn-outline-primary controlButton buttonFont" href="{{ path('admin_registrations') }}">Offene Registrierungen anzeigen</a>
            </div>
            <div class="col-12">
                <a class="btn btn-outline-primary controlButton buttonFont" href="{{ path('admin_export') }}">Exportstring generieren</a>
            </div>
            <div class="col-12">
                <a class="btn btn-outline-primary controlButton buttonFont" href="{{ path('beruf_index') }}">Berufe verwalten</a>
            </div>
            <div class="col-12">
                <a class="btn btn-outline-primary controlButton buttonFont" href="{{ path('eintrittsdatum') }}">Eintrittsdatum bearbeiten</a>
            </div>
        </div>
    </div>

{% endblock %}
