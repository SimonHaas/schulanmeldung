{{ form_start(form) }}
{{ form_errors(form) }}

<div class="row">
    <div class="col-lg-12">
        {{ form_row(form.betrieb, {'attr':{'class':'form-control'}}) }}
        <a class="btn BayreuthButton blankLinkButton btn-sm float-right smallButton" href="{{ path('betrieb_ausbildung') }}">neuen Betrieb anlegen</a>
    </div>
</div>
<div class="row">
    <div class="col-lg-6">
        {{ form_row(form.beginn) }}
    </div>
    <div class="col-lg-6">
        {{ form_row(form.ende) }}
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <br>
        {{ form_row(form.beruf, {'attr':{'class':'form-control'}}) }}
        <br>
    </div>
</div>
<input class="btn BayreuthButton buttonFont btn-block blankLinkButton" type="submit" />

{# TODO: https://github.com/eter    nicode/bootstrap-datepicker ? #}

{{ form_end(form) }}