{% extends 'base.html.twig' %}

{% block title %}Kammer{% endblock %}

{% block body %}
    <h1>Kammer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kammer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kammer.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kammer_index') }}">back to list</a>

    <a href="{{ path('kammer_edit', {'id': kammer.id}) }}">edit</a>

    {{ include('kammer/_delete_form.html.twig') }}
{% endblock %}
